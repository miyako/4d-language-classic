[package]
name = "fourd-language-classic"
version = "0.1.0"
edition = "2021"
description = "Deterministic natural-language lookup service for the 4D classic-language command reference, backed by a build-time-embedded, compiler-verified command IR."
license = "MIT"
default-run = "4d-language-classic"

[[bin]]
name = "4d-language-classic"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tiny_http = "0.12"

[build-dependencies]
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
